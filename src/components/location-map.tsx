"use client";
import { useEffect, useRef, useState } from "react";
import { CircleMarker, MapContainer, Popup, TileLayer } from "react-leaflet";
import { company } from "@/data/company";
const position: [number, number] = [-33.4158667, -70.6917584];
export function LocationMap({ compact = false }: { compact?: boolean }) { const element = useRef<HTMLDivElement>(null); const [visible, setVisible] = useState(false); useEffect(() => { const target = element.current; if (!target) return; const observer = new IntersectionObserver(([entry]) => { if (entry.isIntersecting) { setVisible(true); observer.disconnect(); } }, { rootMargin: "240px" }); observer.observe(target); return () => observer.disconnect(); }, []); return <div ref={element} className={`location-map${compact ? " compact" : ""}`}>{visible ? <MapContainer center={position} zoom={15} scrollWheelZoom={false} aria-label="Mapa de ubicación de Carrocerías HDC-Tiberi"><TileLayer attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" /><CircleMarker center={position} radius={10} pathOptions={{ color: "#07527c", fillColor: "#07527c", fillOpacity: 0.88 }}><Popup><strong>{company.name}</strong>{company.address.street}, {company.address.commune}</Popup></CircleMarker></MapContainer> : <div className="location-map__placeholder"><strong>Mapa de ubicación</strong><span>Se carga al acercarse a esta sección.</span></div>}</div>; }
